import 'package:flutter/material.dart';

class TourSchedulePage extends StatelessWidget {
  const TourSchedulePage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('Tour Schedule')),
      body: const Center(child: Text('Tour Schedule Page')),
    );
  }
}
